<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.army.dao.UserLoginMapper">
      
	<select id="userLogin" parameterType="com.army.vo.UserLogin" resultType="com.army.vo.UserLogin">
	
		SELECT 
			user_name AS userName,
			login_status AS loginStatus
		FROM tb_user_login WHERE user_name=#{userName} AND user_password=#{userPassword}
		
	</select>
	
	<insert id="userRegist" parameterType="com.army.vo.UserLogin">
	    INSERT INTO tb_user_login
	    (
	       user_name,
	       user_password,
	       user_real_name,
	       user_phone,
	       create_time
	    )
	    VALUES
	    (
	        #{userName},
	        #{userPassword},
	        #{userRealName},
	        #{userPhone},
	        GETDATE()
	    )
	</insert>
	
	<select id="findUserLoginByUserName" parameterType="java.lang.String" resultType="com.army.vo.UserLogin">
		
		SELECT
			user_name AS userName,
			user_id AS userId
		FROM tb_user_login WHERE user_name=#{userName}
		
	</select>
      
</mapper>