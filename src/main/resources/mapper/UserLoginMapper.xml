<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.army.dao.UserLoginMapper">
      
	<select id="userLogin" parameterType="com.army.vo.UserInfo" resultType="com.army.vo.UserInfo">
	
		SELECT 
			user_id AS userId,
			user_name AS userName,
			role_name AS roleName
		FROM tb_user_login WHERE user_name=#{userName} AND user_password=#{userPassword}
		
	</select>
	
	<insert id="userRegist" parameterType="com.army.vo.UserInfo">
	    INSERT INTO tb_user_login
	    (
	       user_name,
	       user_password,
	       user_real_name,
	       user_phone,
	       create_time
	    )
	    VALUES
	    (
	        #{userName},
	        #{userPassword},
	        #{userRealName},
	        #{userPhone},
	        GETDATE()
	    )
	</insert>
	
	<select id="findUserLoginByUserName" parameterType="java.lang.String" resultType="com.army.vo.UserInfo">
		
		SELECT
			user_name AS userName,
			user_id AS userId
		FROM tb_user_login WHERE user_name=#{userName}
		
	</select>
      
</mapper>